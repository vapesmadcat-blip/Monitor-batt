<!-- NOVO: 3 Níveis de Voz Configuráveis -->
<TextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Níveis de Voz (configuráveis)"
    android:textColor="#A0C4E2"
    android:textSize="14sp"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="8dp"/>

<!-- Voz Baixa -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Voz Baixa"
        android:textColor="#FFFFFF"/>
    <TextView
        android:id="@+id/tvVoiceLowValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10%"
        android:textColor="#3F9EF0"
        android:textStyle="bold"/>
</LinearLayout>
<SeekBar
    android:id="@+id/seekVoiceLow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:max="80"/>

<!-- Voz Crítica -->
<LinearLayout ... > ... </LinearLayout>
<SeekBar android:id="@+id/seekVoiceCritical" ... />

<!-- Voz Quase Acabando -->
<LinearLayout ... > ... </LinearLayout>
<SeekBar android:id="@+id/seekVoiceVeryLow" ... />
